/**
 * KONTIFY DESIGN SYSTEM - MAIN INDEX
 * 
 * Punto de entrada único para el Design System
 */

// Tokens
export { tokens, colors, typography, spacing, borders, shadows, animations, layout } from './tokens';
export { getColor, getSpacing, getFontSize } from './tokens';

// Atomic Components
export * from './components/atoms';
